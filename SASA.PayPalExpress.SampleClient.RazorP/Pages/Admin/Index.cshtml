@page
@model SASA.PayPalExpress.SampleClient.RazorP.Pages.Admin.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.PayPalBearersToken[0].Token)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PayPalBearersToken[0].DateTimeObtained)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PayPalBearersToken) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Token)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTimeObtained)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
